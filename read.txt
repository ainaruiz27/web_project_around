
      const fieldsetList = Array.from(formElement.querySelectorAll(".form__set"));
  
      fieldsetList.forEach((fieldset) => {




            function esckey(evt) {
  if (evt.keyCode === 27) {
    document.getElementById("tutorial").innerHTML = 'La tecla Escape fue presionada';
    // Aquí colocas la lógica directa para cerrar el popup
    document.getElementById("popup").style.display = "none";
  }
}

// Agregar un listener para el evento de clic en el icono de cierre
const popupCloseIcon = document.getElementById("popupCloseIcon");

if (popupCloseIcon) {
  popupCloseIcon.addEventListener("click", function () {
    // Cerrar el popup cuando se hace clic en el icono de cierre
    cerrarPopup();
  });
}

// Agregar un listener para el evento de tecla presionada
document.addEventListener("keydown", esckey);
